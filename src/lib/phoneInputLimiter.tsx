// Deprecated: no longer used (replaced by Phone10Field which hard-limits to 10 digits).
// Keeping file to avoid breaking imports in history, but it's safe to remove once all usages are gone.

export {};
